export default class ArrivalPrediction {
    constructor(lineName, destinationName, timeToStation) {
        this.lineName = lineName;
        this.destinationName = destinationName;
        this.timeToStation = timeToStation;
    }
}
